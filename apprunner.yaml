version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - cd server && npm install && npm run build
run:
  command: cd server && npm start
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: production
    - name: AWS_REGION
      value: us-east-1
